server:
  port: 8082
spring:
  application:
    name: Minhas Financas
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/minhasfinancas?allowPublicKeyRetrieval=true&sslMode=DISABLED
    username: root
    password: root